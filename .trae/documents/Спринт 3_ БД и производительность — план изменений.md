## Задачи
- Добавить индексы по частым фильтрам: rounds(tournament_id), matches(round_id), tournament_participants(tournament_id), leaderboard(tournament_id).
- Ввести уникальность ника в рамках турнира: UNIQUE (tournament_id, nickname) для tournament_participants.
- Минимизировать риски: изменения только в миграционном SQL, без влияния на RLS, API и UI.

## Реализация
- Обновить файл миграций: добавить CREATE INDEX IF NOT EXISTS для 4 таблиц и UNIQUE INDEX IF NOT EXISTS на (tournament_id, nickname).
- Не менять существующие политики RLS и схему полей.

## Критерии
- Запросы по указанным FK не выполняют full scan.
- Попытка добавить участника с уже занятым ником внутри турнира возвращает ошибку БД (обрабатывается текущим кодом).
- Никаких регрессий в API/страницах.